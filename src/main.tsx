import React from 'react';
import { useApi } from '@/lib/api-context';

// Main.tsx file for the v6 trading platform
const main = () => {
  return (
    <React.StrictMode>
      <App />
    </React.StrictMode>
  );
};

export default main;
